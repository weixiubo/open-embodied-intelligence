# API keys
DEEPSEEK_API_KEY=sk-94f98c13075d4430a69be955d7d89ef0
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

BAIDU_API_KEY=Uk52iXfgR0J6kvo7MTAJYhfc
BAIDU_SECRET_KEY=fsWeCc6p2AhpZUfVi4w40CiAggEe0ocu

# Runtime
DEBUG=false
LOG_LEVEL=INFO
LOG_TO_FILE=false
SILENT_STARTUP=false

# Serial
SERIAL_PORT=/dev/ttyUSB0
SERIAL_BAUDRATE=115200
SERIAL_AUTO_DETECT=true
SERIAL_REQUIRED=false

# Optional Baidu tuning
BAIDU_TTS_VOLUME=8
BAIDU_TTS_VOICE=5

# 麦克风设备索引（默认不设置则使用系统 default）
# 用以下命令查询可用索引：
#   python3 -c "import pyaudio; p=pyaudio.PyAudio(); [print(i, p.get_device_info_by_index(i)['name']) for i in range(p.get_device_count()) if p.get_device_info_by_index(i)['maxInputChannels']>0]; p.terminate()"
# 设备列表示例中 index=0 是第一个 USB 麦克风：
# AUDIO_INPUT_DEVICE_INDEX=0
