# OpenEI Dance Demo

This example runs the legacy dance capability on top of the OpenEI runtime.

## Interactive text mode

```bash
openei run --transport sim --input-mode text
```

## One-shot scripted mode

```bash
openei run --transport sim --text "跳舞十秒" --once
```

## Inspect runtime

```bash
openei inspect --transport sim
```

